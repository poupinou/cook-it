<div>
  <h1>Ajouter une recette</h1>

  <%= form_for @recipe do |rec| %>
       <%= rec.hidden_field :user_id, value: @user.id %>
    <p><%= rec.text_field :name, placeholder: 'Nom de la recette' %></p>
    <p><%= rec.text_area :description, placeholder: 'Description de la recette' %></p>
    <p><%= rec.file_field :picture, placeholder: 'URL de la recette' %></p>
    <p><%= rec.text_field :time, placeholder: 'Temps de préparation' %></p>
    <p><%= rec.text_field :price, placeholder: 'Prix moyen' %></p>

    <p><%= rec.submit 'Ajouter cette recette' %></p>
  <% end %>

</div>
