<div class="profil-g">
  <center class="profil-head">
  	<h1>Ma Liste de Course</h1>
  	<p>cette page , c'est la tienne prend en soin ;)</p>
  </center>

  <div class="profil-corps">
  	<center class="liste">
  		<h1>Ma Liste de course</h1>

        <% @list_ing_user.each do |ing| %>
          <p><%=@text =  Ingredient.find(ing.ingredient_id).name %> - <%= ing.quantity %></p>
        <% end %>

      <p><%= button_to 'Annuler ma liste de course', liste_reset_path %></p>

  	</center>
          <%= form_tag send_sms_path, methode: :post do %>
          <%= text_field_tag :phone_number %>
          <%= submit_tag("send by text") %>
          <% end %>
          <%= link_to 'send by email', send_mail_path %>
  </div>
</div>
